---
interface Props {
	dark?: boolean;
}

const { dark } = Astro.props;

// dark mode
let id_color = dark ? 'dark' : 'gray';
---

<hr
	class='w-[95%] mx-auto my-4 border-2 rounded-lg'
	style={`border-color: var(--${id_color});`}
/>
