---
interface Props {
  dark?: boolean;
}

const { dark } = Astro.props;

// dark mode
let id_color = dark ? 'dark' : 'gray';
---

<hr
  class='w-full mx-auto border-2 rounded-full mt-4'
  style={`border-color: var(--${id_color});`}
/>
