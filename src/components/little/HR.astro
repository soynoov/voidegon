---
interface Props {
	dark?: boolean;
}

const { dark } = Astro.props;

// dark mode
let id_color = dark ? 'dark' : 'gray';
---

<hr
	class='w-[99%] mx-auto border-2 rounded-full mb-2'
	style={`border-color: var(--${id_color});`}
/>
